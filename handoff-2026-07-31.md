# CLARVIS — Handoff 2026-07-31 (finish the night's work)

**Paste this whole file into a fresh Claude Code session.** It's written for a
Claude that has never seen this project, working with Alex at his Mac.

> **Verify before acting.** This project's own hard-won lesson: status docs drift.
> Tonight two "open" items from older handoffs turned out to be long done. Check
> live state before trusting any claim below, including mine.

---

## 0. The one-paragraph situation

CLARVIS is Alex's personal AI second brain: a Flask app (`~/second-brain/second-brain-chat/`)
with a Claude tool-use chat brain, a sci-fi HUD front end, voice, autonomous "managed
tasks", unified intake (mail/iMessage/calendar), proactive phone nudges, and a
self-expansion pipeline. Mac = home node; a Hetzner box behind Coolify = always-on server.
Everything persists in ONE Supabase table (`Agent Outputs`) tagged by `agent_name`.

**Last night's session shipped 17 commits and took the suite from 253/2 → 435/0.**
**None of it is live.** The server has not deployed since **2026-07-25** — it is
**30+ commits behind**. That is job #1 and it gates everything else.

---

## 1. 🔴 FIRST: the deploy (nothing else matters until this is done)

### What's proven
The live server runs commit `0efd2a7` (2026-07-25). This was established **without
SSH** by a trick worth reusing: **static files bypass the login gate**, so you can
fingerprint the deployed code.

```bash
cd ~/second-brain
curl -s https://clarvis.178.156.209.40.sslip.io/static/hud/hud.js -o /tmp/srv.js
for c in $(git log --format=%h -25 -- second-brain-chat/static/hud/hud.js); do
  git show "${c}:second-brain-chat/static/hud/hud.js" > /tmp/h.js 2>/dev/null
  cmp -s /tmp/h.js /tmp/srv.js && echo "SERVER IS ON: $c" && break
done
```

### What is NOT the problem
Infra is healthy: host pings, port 22 open, Coolify UI (`:8000`) answers, the app serves.
Coolify is **not** down. GitHub has every push.

### Best hypothesis
`766819e` (07-25) added `pyautogui`/`pynput` to requirements → breaks a headless Linux
build. `821d11c` (07-26) fixed it with `sys_platform == "darwin"` markers — **but that
fix is itself undeployed**, so the cure is stuck behind the disease. Three separate
"re-trigger deploy" commits (07-25, 07-28, 07-31) never took, which suggests deploys
aren't **triggering**, not merely failing. Current requirements were verified
server-safe, so a manual redeploy should succeed.

### Alex's steps (Coolify UI — Claude has no credentials and must never receive any)
1. Coolify → app `second-brain-chat` (uuid `h72tei3gy97z4wlqyqpvuylg`) → **Deployments**
2. Any attempt since 07-25? **Yes** → read the build log. **No** → it's the trigger.
3. Check the app's **auto-deploy / webhook** setting is still enabled ← main suspect
4. If dead, reconnect the GitHub source (App "second-brain1")
5. **Redeploy**
6. **Verify:** `curl https://clarvis.178.156.209.40.sslip.io/api/version`
   → must return `{"commit": "8c1ceff"…}` or later. **A 401 means it did NOT take.**

> `/api/version` is new and ungated precisely so deploy checks never need SSH again.
> If it 401s, the old code is still running.

### Gotchas
- **The Coolify deployments list lies.** It has shown "Failed" for deploys that
  actually succeeded, with timestamps ticking forward. Trust `/api/version`, or
  `docker exec` into the container — never the UI's word.
- **Two unpushed commits** sit on the Mac (`8854c6c`, `8c1ceff`) — see §3.
- Browser automation against the Coolify UI is historically unreliable (Alpine.js
  click handlers). Have Alex click, don't automate it.

---

## 2. 🟡 Then: the things only Alex can do

Full detail in `~/second-brain/NEEDS_ALEX.md`. Summary:

| # | Item | What Claude does |
|---|---|---|
| 1 | **macOS Accessibility + Screen Recording** (System Settings → Privacy & Security) — `AXIsProcessTrusted` is `False`, so screen control physically cannot click | After: verify with `python3 -c "from ApplicationServices import AXIsProcessTrusted; print(AXIsProcessTrusted())"` |
| 2 | **Talk to voice conversation mode** — tap mic ONCE, speak naturally | Tune `SILENCE_MS` / `MIN_SPEECH_MS` in the VAD block of `templates/index.html` from his feedback. Cuts him off → raise `SILENCE_MS`; laggy → lower it |
| 3 | **Go/no-go on `screen_agent.py`** | If yes: 3 lines, documented at the bottom of that module. A test currently pins it as unwired — update `suite_screen_agent` too |
| 4 | **`GITHUB_TOKEN`** into `.env` + Coolify (scout: 10/min → 5000/hr) | — |
| 5 | **`ssh-copy-id root@178.156.209.40`** — there are NO ssh keys on this Mac; access is password-only, which blocked a restart Alex authorized last night | — |
| 6 | 4 stale expansion findings in the review panel (pre-fix job scrapers) | Dismiss on his say-so |

---

## 3. What last night actually built (all suite-green, none live)

**15 pushed** (`20f9264` → `afa40db`), **2 unpushed on the Mac** (`8854c6c` SSRF fix,
`8c1ceff` the needs-list). The 2 were deliberately held under a "deploy freeze" — do
not push more until the batch is verified live, then push them.

Themes, newest first:
- **HUD phone layout** — the whole 1600×1000 scene used to scale to a postage stamp on
  Alex's phone (where the PWA lives). Under 700px it's now a stacked column of the same
  widgets; desktop untouched. Chat bar also stopped requiring a second Send (`go=1`).
- **Smoothness** — managed-task worker had NO prompt caching (30 rounds × ~90 uncached
  tool schemas); chat history prefix caching; cross-account mail dedupe (the same email
  via school Gmail + iCloud made two triage items).
- **Silent-failure cure** — `/api/version`; heartbeats where each subsystem declares its
  own staleness and a quiet one buzzes Alex's phone via ntfy.
- **Truncation bug class, eradicated** — the expansion scout had silently found NOTHING
  for 8 days (three stacked bugs: `_extract_json` mangling single-element arrays into
  dicts, thinking blocks eating the whole `max_tokens`, swallowed errors). Same class
  found and fixed in `task_manager` (the **guardrail council** — now fails CLOSED) and
  `money_pipeline`.
- **Scout re-aimed** at real capability gaps (open FRICTION items, failing tools) instead
  of recent task topics.
- **Draft survival** — `draft_store.py` mirrors self-authored drafts to Supabase so
  redeploys stop wiping them; `retention.py` keeps the shared table bounded (whitelist-only).
- **Security** — SSRF hole closed in `web_fetch` (see `VIBE_CODE_AUDIT.md`).

Reports worth reading: `VIBE_CODE_AUDIT.md`, `TOOL_AUDIT.md`, `NEEDS_ALEX.md`.

---

## 4. House rules (violating these is worse than doing nothing)

- **Human gate is sacred.** Nothing CLARVIS writes for itself goes live without Alex
  passing on it once. Hard gates (money, accounts, external sends, file deletion) are
  structural — never route around them.
- **Remote shell on the Hetzner box** requires Alex naming the exact command AND host in
  his own message. A "yes" to an offer doesn't count.
- **Secrets: env vars only** (root `~/second-brain/.env`, *not* `second-brain-chat/.env`).
  Never in chat, never typed into a form, never into Coolify by Claude.
- **Verify what ships, not what compiles.** Last night, loading pages in a real browser
  and running live API calls caught bugs that syntax checks and a green suite missed
  (a hanging `getUserMedia`, a src-layout smoke test, a cached template).
- **Every fix gets a regression test** in `run_tests.py` (`python3 run_tests.py`,
  `--only=<suite>` to filter). Loosening a guard requires negative-testing both halves.
- **Sacred patterns:** modular tool shape (schema + function + dispatch + status label);
  `HUD_STYLE.md` governs all UI; don't "clean up" the HUD aesthetic.
- **Narrate what you're doing and why.** Alex is learning the system as it's built, and
  prefers plain-language summaries.

---

## 5. Definition of done for this handoff

1. `curl .../api/version` returns `8c1ceff` or later
2. Phone HUD shows the stacked column, not a postage stamp
3. Voice conversation mode tested by Alex; thresholds tuned if needed
4. Accessibility granted; screen control starts without refusing
5. The 2 unpushed commits pushed and deployed
6. Heartbeats quiet (or their alerts triaged) 24h later

Then the next frontier — per an independent review last night — is **daily-use
value**, not new capability: intake triage, latency, and voice are what make CLARVIS
worth opening. A tool prune (93 registered, 12 used) waits on ~a week of cross-node
audit data; `app.py` at 5,600 lines should be split during that same job.
