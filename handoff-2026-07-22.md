# CLARVIS — Handoff 2026-07-22 (MASTER BUILD: "Jarvis becomes the real assistant")

**Read me first, then `handoff-2026-07-21.md`** — that doc's capability inventory (§2),
stack (§3), and working principles (§6) remain valid; THIS doc supersedes its state (§4)
and open items (§5). Verify load-bearing claims against repo/Coolify/Supabase before acting.

## What today was

The pivot session from "many features" to "one excellent assistant." Alex present
throughout; every phase live-proven on his phone. All pushed through `07edfb2`;
full suite **255/0** + standalone suites 42+27+57+43+18, all green.

## New since yesterday (all live on the server unless marked Mac-only)

1. **Vault sync self-heals iCloud content eviction** (`da11aac`) — iCloud evicts vault
   FILE content (APFS dataless), git add dies with EDEADLK. vault_sync.sh now detects
   (`find -flags +dataless`) + materializes (`brctl download`, 60s wait) before git.
   The .git-dir migration was ALREADY done 2026-07-19 (pointer → ~/.second-brain-vault.git).
2. **Internet-facing auth hardened** (`a1654d3`) — `login_limiter.py`: 5 wrong/IP → 15-min
   lock, 20 global → all locked, checked BEFORE compare (right code also 429 during lock);
   trips write a system_event. ProxyFix under TRUSTED_PROXY_COUNT=1 (server); cookies
   HttpOnly/SameSite=Lax/Secure-on-server. SECURITY_NOTES §9. Proven live: 5×200→429.
3. **HTTPS** — app now at **https://clarvis.178.156.209.40.sslip.io** (Let's Encrypt via
   Coolify; issued fine despite the sslip.io rate-limit warning; valid to 2026-10-20).
   Old UUID hostname retired. PWA installed on Alex's phone (no service worker — every
   open loads the current deploy).
4. **Unified intake layer** (`103ee9e`, tuned `59124b4`) — `intake.py`: one normalized,
   noise-filtered, deduped event stream (Supabase rows `intake_event`/`intake_state`) ←
   iMessage (`imessage_intake.py`, Mac-only, chat.db READ-ONLY mode=ro, typedstream
   decode, conversation context, cursor backfill), Gmail + Calendar scans (whitelisted
   read-only Composio via dispatcher), and `capture_intake` paste-inbox (the fallback for
   school/workouts/anything). Triage: dashboard panel #1 with one-tap Accept→tasks /
   Dismiss (`/api/intake/act`) + chat tools. Extraction prompt knob: `INTAKE_PROMPT_RULES`.
5. **Proactive engine** (`0b4f92b`) — `proactive.py`: deterministic awareness pass
   (due-soon, intake pile-up, morning-brief/evening-review windows) → **ntfy.sh** push
   (topic in env `NTFY_TOPIC`, subscribed on Alex's phone) under respect rules (quiet
   hours, max/day, never-twice keys; `jarvis_nudge` ledger; dashboard settings panel +
   `/api/notifications`). 15-min worker runs SERVER-side (`PROACTIVE_LOCAL=1` to also
   run locally). Live-proven: 4 real nudges incl. a same-day dentist deadline.
6. **Phase 6 lite** (`07edfb2`) — FRICTION.md + `log_friction` tool + POLISH_PROMPT.md
   (weekly polish ritual); expansion scout ON daily (job-queue "scout" handler + hourly
   server scheduler, once/day after 6am, doubly budget-gated, state in Supabase).

## Server/env facts (Coolify app h72tei3gy97z4wlqyqpvuylg)

Env added today by Alex/me in Coolify: `TRUSTED_PROXY_COUNT=1`, `SESSION_COOKIE_SECURE=1`,
`NTFY_TOPIC` (same value as local .env — one topic, both nodes). Webhook auto-deploys on
push to main; deploys took ~50s today (the 2026-07-21 memory's "frozen log ≠ dead build"
lesson still applies under load). Mac = home node (iMessage/screen/say/whisper);
server = always-on brain (proactive worker, daily scout, chat).

## Next session opens on (in order)

1. **Phase 5, skipped today entirely**: latency pass (measure send→first-token first),
   voice round-trip, UI polish with Alex driving (his "vibecoded" complaint — spend real
   time; log_friction everything he says).
2. **Tedious-task templates** — get Alex's top-5 recurring tedious tasks (never asked),
   each a propose→approve template.
3. Deeper cross-feature linking (P3.2 remainder); server persistence for
   agents/proposed_tools/vault_inbox (still ephemeral); budget cap still placeholder $20
   (Alex declined again implicitly today); GITHUB_TOKEN still unset (optional).
4. Watch: first daily scout run on the server (~6am local tomorrow) — findings should
   appear in the Expansion panel; intake noise-filter tuning as real traffic flows
   (`INTAKE_PROMPT_RULES`); iMessage backlog draining at 25/poll.

## Safety deltas today (SECURITY_NOTES.md updated)

New surfaces: internet-facing login (hardened, §9), iMessage read-only reader (mode=ro,
no send path exists), ntfy push channel (topic name = the only secret; nudge text kept
terse — no message bodies), intake extraction (all source text through the untrusted
boundary; instructions in content are surfaced, never followed).
